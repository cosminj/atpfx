CREATE TABLE CANDLES
(
  ID             NUMBER(9),
  SIGNALPROVIDER_ID  NUMBER(3),
  PAIR_ID        NUMBER(3),
  INTERVAL_ID    NUMBER(3),
  OPEN_TICK_ID   NUMBER(9),
  LOW_TICK_ID    NUMBER(9),
  HIGH_TICK_ID   NUMBER(9),
  CLOSE_TICK_ID  NUMBER(9)
)
TABLESPACE USERS
PCTUSED    0
PCTFREE    10
INITRANS   1
MAXTRANS   255
STORAGE    (
            INITIAL          64K
            MINEXTENTS       1
            MAXEXTENTS       2147483645
            PCTINCREASE      0
            BUFFER_POOL      DEFAULT
           )
LOGGING 
NOCOMPRESS 
NOCACHE
NOPARALLEL
MONITORING;


CREATE UNIQUE INDEX CANDLES_PK ON CANDLES
(ID)
LOGGING
TABLESPACE USERS
PCTFREE    10
INITRANS   2
MAXTRANS   255
STORAGE    (
            INITIAL          64K
            MINEXTENTS       1
            MAXEXTENTS       2147483645
            PCTINCREASE      0
            BUFFER_POOL      DEFAULT
           )
NOPARALLEL;


CREATE OR REPLACE TRIGGER "BI_CANDLES"  
  before insert on "CANDLES"              
  for each row
begin  
    select "CANDLES_SEQ".nextval into :NEW.ID from dual;
end;
/
SHOW ERRORS;



ALTER TABLE CANDLES ADD (
  CONSTRAINT CANDLES_PK
 PRIMARY KEY
 (ID)
    USING INDEX 
    TABLESPACE USERS
    PCTFREE    10
    INITRANS   2
    MAXTRANS   255
    STORAGE    (
                INITIAL          64K
                MINEXTENTS       1
                MAXEXTENTS       2147483645
                PCTINCREASE      0
               ));


ALTER TABLE CANDLES ADD (
  CONSTRAINT CANDLES_FK 
 FOREIGN KEY (SIGNALPROVIDER_ID) 
 REFERENCES SIGNALPROVIDERS (ID));

ALTER TABLE CANDLES ADD (
  CONSTRAINT CANDLES_FK2 
 FOREIGN KEY (PAIR_ID) 
 REFERENCES PAIRS (ID));

ALTER TABLE CANDLES ADD (
  CONSTRAINT CANDLES_FK3 
 FOREIGN KEY (INTERVAL_ID) 
 REFERENCES INTERVALS (ID));

ALTER TABLE CANDLES ADD (
  CONSTRAINT CANDLES_FK4 
 FOREIGN KEY (OPEN_TICK_ID) 
 REFERENCES TICKS (ID));

ALTER TABLE CANDLES ADD (
  CONSTRAINT CANDLES_FK5 
 FOREIGN KEY (LOW_TICK_ID) 
 REFERENCES TICKS (ID));

ALTER TABLE CANDLES ADD (
  CONSTRAINT CANDLES_FK6 
 FOREIGN KEY (HIGH_TICK_ID) 
 REFERENCES TICKS (ID));

ALTER TABLE CANDLES ADD (
  CONSTRAINT CANDLES_FK7 
 FOREIGN KEY (CLOSE_TICK_ID) 
 REFERENCES TICKS (ID));




